# Local Development Environment Variables

# Database (set POSTGRES_PASSWORD before running docker-compose)
POSTGRES_PASSWORD=change_me_local_dev
DATABASE_URL=postgres://udp:change_me_local_dev@localhost:5432/udp

# Auth secret (REQUIRED — generate with: openssl rand -base64 32)
AUTH_SECRET=

# Session (generate with: openssl rand -base64 32)
SESSION_SECRET=your-local-session-secret-change-in-prod

# Bootstrap admin credentials (used by POST /api/auth/bootstrap in dev)
BOOTSTRAP_ADMIN_EMAIL=admin@local
BOOTSTRAP_ADMIN_PASSWORD=

# Cron job secret (required in production for /api/cron/* endpoints)
CRON_SECRET=

# AI Configuration
# Set AI_PROVIDER to: mock (default), openai, or anthropic
AI_PROVIDER=mock
AI_MODEL=gpt-4o-mini

# OpenAI API Key (required if AI_PROVIDER=openai)
# Get your key at: https://platform.openai.com/api-keys
OPENAI_API_KEY=

# Anthropic API Key (required if AI_PROVIDER=anthropic)
# Get your key at: https://console.anthropic.com/
ANTHROPIC_API_KEY=

# Stripe (optional for dev)
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=

# Marketing Channel OAuth (optional - for channel integrations)
# Meta (Instagram/Facebook) - https://developers.facebook.com/
META_APP_ID=
META_APP_SECRET=

# Google (Google Ads) - https://console.cloud.google.com/
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# TikTok - https://developers.tiktok.com/
TIKTOK_CLIENT_KEY=
TIKTOK_CLIENT_SECRET=

# LinkedIn - https://www.linkedin.com/developers/
LINKEDIN_CLIENT_ID=
LINKEDIN_CLIENT_SECRET=

# SendGrid (Email) - https://sendgrid.com/
SENDGRID_API_KEY=

# Twilio (SMS/WhatsApp) - https://www.twilio.com/
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
TWILIO_PHONE_NUMBER=

# App URL (for OAuth callbacks)
NEXT_PUBLIC_APP_URL=http://localhost:3000
