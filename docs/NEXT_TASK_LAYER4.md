Next: Layer 4 Master Data and Dimensions

Goal:
- parties + party_profiles + party_identifiers
- dimension_definitions + dimension_values + entity_dimensions
- minimal CRUD routes under /api/master/*
- audit_events for create/update/tag
- no ledger writes outside src/lib/posting.ts
- tenant scoped everywhere

Use the Layer 4 JSON super prompt we prepared.
